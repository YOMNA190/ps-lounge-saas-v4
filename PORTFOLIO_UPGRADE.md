# PS Lounge Portfolio Upgrade

## موضع المشروع

يحتوي المشروع بالفعل على أساس قوي: أجهزة، جلسات، عملاء، مخزون، ورديات، اشتراكات، بطولات، سجل تدقيق، بوابة عميل، وشاشة عامة. الهدف الآن هو إظهار هذه القدرات ضمن تجربة Product Story واحدة يمكن تقديمها في المقابلة، مع التفريق بين ما هو متصل في الواجهة وما يحتاج تحققاً كاملاً من قاعدة Supabase.

## سيناريو العرض

ابدأ بفرع مزدحم. أنشئ حجزًا لجهاز، ثم ابدأ جلسة، وأضف طلبًا من المخزون، وانقل الجلسة إلى الفاتورة، وأغلق وردية الموظف. تستخدم الجلسات اشتراك Supabase قابل للتصفية بالفرع، بينما تُحدّث شاشة العرض العامة بالاستعلام الدوري. يوضح ذلك التعامل مع الوقت والحجوزات والمخزون والتحصيل والصلاحيات في نظام واحد، ولا ينبغي وصف كل الشاشة بأنها لحظية قبل اختبار مخطط Supabase والإعدادات الفعلية.

## معايير الترقية

- [x] إزالة أخطاء الاستخدام غير الضروري وإبقاء تحذيرات React Hooks ظاهرة بدلاً من تعطيلها؛ `npm run lint` يمر حالياً مع تحذيرات موروثة تحتاج معالجة لاحقة.
- [x] إضافة اختبارات لمنع تداخل الحجوزات وحساب الجلسة وإغلاق الوردية.
- [x] إضافة صفحة دراسة حالة تشرح حالة الفرع متعدد الأجهزة والقرارات المعمارية.
- [ ] توثيق نموذج البيانات وعقود Supabase والصلاحيات والمخاطر التشغيلية.
- [ ] تحسين Code Splitting لتقليل حجم الحزمة الإنتاجية الحالية.

## تحقق الجودة الحالي

| الأمر | النتيجة | الملاحظة |
|---|---|---|
| `npm test` | ناجح: 3 اختبارات | يغطي تعارض حجز الجهاز ضمن الفرع، سقف الخصم في الفاتورة، وفارق تسوية الوردية. |
| `npm run build` | ناجح | قُسمت حزم React وSupabase وCharts، لكن حزمة التطبيق الرئيسية ما زالت تقارب 754KB بعد الضغط. |
| `npm run lint` | ناجح مع تحذيرات | تحذيرات React Hooks موروثة مرئية في مكونات تعتمد على تحميل بيانات داخل `useEffect`؛ لم تُخفَ ولم تُحوّل إلى ادعاء اكتمال. |

## ما تدعمه الشفرة وما يحتاج تحققاً

يمتلك المشروع سياق فرع يحمّل الفرع من `profile.branch_id`، كما تقبل اشتراكات الجلسات فلتر `branch_id`. هذا **أساس لدعم الفروع** وليس دليلاً على أن كل قراءة أو RPC أو سياسة صف في Supabase معزولة لكل فرع؛ يلزم توثيق المخطط وسياسات RLS واختبارها. تستدعي الجلسات والورديات والفواتير إجراءات RPC بالاسم، لكن نجاحها التشغيلي يعتمد على وجود هذه الإجراءات وقواعد الصلاحيات في بيئة Supabase المستهدفة.

| التدفق | نقطة الواجهة | العقد الموجود في الشفرة | حد التحقق الحالي |
|---|---|---|---|
| الفروع | `branch-context.tsx` | تحميل `profile.branch_id` وبيانات `branches` | لا يثبت أن كل استعلام أو RLS مفصول بالفرع. |
| الجلسة والحجز | `StartSessionModal` و`DeviceCard` | `start_session`، `stop_session_with_bill`، و`subscribeToSessions` مع فلتر فرع اختياري | اختبارات الوحدة تثبت تعارض الجهاز في الفرع وتسعير الجلسة؛ RPC وسياسات قاعدة البيانات غير مختبرة end-to-end هنا. |
| الفاتورة والطلب | `SessionBillModal` و`AddOrderToSessionModal` | `get_session_bill`، `add_order_to_session`، `stop_session_with_bill` | منطق الخصم محمي في اختبار وحدة؛ التسوية النهائية تعتمد على RPC الخارجي. |
| الوردية | `ShiftsPage` | إنشاء صف `shifts` وRPC باسم `end_shift` | اختبار الوحدة يثبت معادلة تسوية النقد؛ PIN والدفتر النقدي يتطلبان تحققاً في Supabase. |
| الشاشة العامة | `PublicDisplayPage` | استعلام دوري كل 30 ثانية للأجهزة والجلسات وقائمة الانتظار | ليست قناة لحظية كاملة؛ لا تُعرض على أنها كذلك. |

تفاصيل التحذيرات الموروثة، مرتبة حسب الملفات وخطوة المعالجة التالية، موجودة في [`LINT_DEBT.md`](./LINT_DEBT.md).
